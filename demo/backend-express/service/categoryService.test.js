const categoryService = require('./category.service');

//ToDo Define beforeEach, afterEach functions, mock part with DB, fix issue with 'Error: Encoding not recognized: 'cesu8' (searched as: 'cesu8')'

describe('Return all categories', () => {
    test('should_find_categories', () => {

    });
});